plugins {
    id "com.android.application"
}

android {
    namespace "dev.codex.lyricatagger"
    compileSdk 36

    defaultConfig {
        applicationId "dev.codex.lyricatagger"
        minSdk 29
        targetSdk 36
        versionCode 1
        versionName "0.1.0"
    }
}
